name: epp
author: scalio

difficulty: ezmed

description: |-
  Dlog in Ecc can be broken , so i fixed it 

  `{{ nc }}`

flag: Cybears{ELL_EXTENSION}

provide:
  - ./challenge/server.py

containers:
  main:
    challenge:
      build: ./challenge
      ports: [1025]

expose:
  main:
    - target: 1025
      tcp: epp.ctf.clawtheflag.com

deployed: false
visible: true
